import React from 'react'

export const Loading = () => {
    return (
        <div style={{ maxHeight: "100vh", display: "flex", justifyContent: "center", alignItems: "center", transform: "translateY(45vh)", color: "whitesmoke" }}>
            <h2>
                Loading....
            </h2>
        </div>
    )
}
